import {
} from '../constants/actions';

import {
} from './AppReducer';

export default (reduction, action) => {
  switch (action.type) {
    // no actions yet
  default:
    return reduction;
  }
}
